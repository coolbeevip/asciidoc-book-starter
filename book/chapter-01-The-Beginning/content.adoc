= 序章

我的名字是 {author}。
这里有一段示例文本。

[.text-center]
_未来已经到来，但仍有人抱着传统数据库不放。_

你觉得调用 `fastify.ready()` 合适吗，还是应该改用 `fastify.listen()`？

Docker 允许把整套技术栈打包进一个容器：

* 操作系统
** Windows
** Ubuntu
* JVM
* 应用服务器
* 包含配置的应用程序
